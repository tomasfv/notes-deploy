import { Router } from 'express';

const router = Router();

// Category routes will be defined here
// Example: router.get('/', getCategories);
// Example: router.post('/', createCategory);

export default router;
