import { Router } from 'express';

const router = Router();

// Note routes will be defined here
// Example: router.get('/', getNotes);
// Example: router.post('/', createNote);

export default router;
